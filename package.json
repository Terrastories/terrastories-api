{
  "name": "terrastories-api",
  "version": "1.0.0",
  "description": "TypeScript backend for Terrastories geostorytelling platform",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:seed": "tsx src/db/seed.ts",
    "prepare": "husky",
    "validate": "npm run type-check && npm run lint && npm test"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "@fastify/swagger": "^9.4.0",
    "@fastify/swagger-ui": "^5.2.3",
    "@fastify/cors": "^10.0.0",
    "@fastify/helmet": "^13.0.0",
    "drizzle-orm": "^0.38.0",
    "postgres": "^3.4.7",
    "better-sqlite3": "^11.8.0",
    "zod": "^3.24.0",
    "drizzle-zod": "^0.8.3",
    "dotenv": "^16.5.0",
    "zod-to-json-schema": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0",
    "@types/better-sqlite3": "^7.6.0",
    "tsx": "^4.20.4",
    "vitest": "^3.2.4",
    "@vitest/coverage-v8": "^3.2.4",
    "drizzle-kit": "^0.30.0",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.19.0",
    "prettier": "^3.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0"
  },
  "keywords": [
    "terrastories",
    "typescript",
    "fastify",
    "drizzle",
    "geostorytelling"
  ],
  "author": "Terrastories Team",
  "license": "MIT",
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write",
      "npm run type-check"
    ],
    "*.{js,json,md}": [
      "prettier --write"
    ]
  }
}
